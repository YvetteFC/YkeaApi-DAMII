spring:
  application:
    name: ykeasMSPrincipal
  config:
    import: optional:environment.yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        #dialect: org.hibernate.dialect.MySQL5InnoDBDialect
server:
  port: 8080